---
project: OstadLagbo
type: change-log
status: current
updated: 2026-09-26
id: OL-CHG-001
owner: Iftikher
---

# Change Log

Records every founder-approved change to the MVP scope baseline. Newest first. Each entry: what changed, why, where it now lives, and which baseline version absorbed it.

| ID | Date | Change | Rationale | Absorbed in |
|---|---|---|---|---|
| CL-049 | 2026-09-26 | **Cloudflare deferred until a domain exists (founder decision).** ADR-001 places Cloudflare at the edge for NFR-05's requirement (d) — request-log control and rate limiting — and `design-foundations` requires the app's base URL to be the Cloudflare-proxied domain rather than the raw `*.onrender.com` host. **No domain has been bought, so there is no edge.** Recorded as a deferral with a trigger rather than left as an assumption: the raw Render host is the base URL for **Slices 0–3**, and Cloudflare goes in **before soft launch**. Two consequences written down rather than discovered later: the threat model's main mitigations (T-1 scraping, T-4 OTP abuse) have **no edge enforcement at all** meanwhile, so those limits must be implemented **inside the API** (register S2-1, S0-12); and the register's Cloudflare items (S0-6, S0-8) are marked as waiting on it instead of reading as things already possible. Also noted: the domain gates **three** things — the edge, the **web account-deletion page Google Play mandates**, and Apple's organization enrolment if an entity is registered | The open-items register listed *verifying* Cloudflare's behaviour while nothing had put Cloudflare in place — an assumption inherited from ADR-001 and never checked. Found while reviewing the Figma design for Slice 0 build-readiness, from `design-foundations`' base-URL rule | **Baseline v1.3** (pending); OL-OPN-001, OL-UI-003, NFR-05, OL-THR-001 |
| CL-048 | 2026-09-26 | **Analytics event catalog reconciled with the shipped code (correction).** CL-040's catalog proposed a `subject.verb_past` naming convention and a name set written **without reading the code repository**, where six client events were already shipped in `packages/shared/src/events.ts` as flat `lower_snake_case` (`map_session_start`, `map_viewport_search`, `category_search`, `zero_result_search`, `share_tap`, `screen_view`). **By this catalog's own no-rename rule the shipped names win** — they are marked *shipped* and locked. Eleven further client events are added in the same convention to carry MAP-11's remaining obligations, and every server-side event is renamed to match. The catalog now also records that adding an event is a **two-file change** — the catalog and `CLIENT_EVENT_NAMES` — because `POST /v1/events` silently drops any name absent from that constant | Renaming a shipped event breaks every historical series, which is the rule the first draft asserted and then violated. Found while reviewing the code repository for build-readiness | **Baseline v1.3** (pending); OL-CAT-EVT-001 |
| CL-047 | 2026-09-26 | **Open-items register created (OL-OPN-001).** Every unresolved item pulled into one file, ordered by **when it must be resolved**: 2 items due now (the entity decision and its 30-day D-U-N-S clock; the Play account), 12 in Slice 0, 2 in Slices 1–2, 8 before soft launch, 3 before public launch, 3 pending v1.3 decisions, and 5 standing risks. Each names its source. The register is **authoritative** where a source disagrees. ADR-001's own open-items list is recorded as stale — three of its eight are done (SMS gateway → Alpha SMS; API framework → TypeScript/Node; Apple account exists) — by amendment, since accepted ADRs are immutable | The items were spread across eight documents for one solo builder to track, and ADR-001's list had already drifted out of date without anyone noticing — the exact failure the register exists to prevent. Raised while reviewing build-readiness | **Baseline v1.3** (pending); OL-OPN-001, ADR-001 |
| CL-046 | 2026-09-26 | **UI states completed across the remaining 15 screens (documentation).** A build-readiness review found CL-045 had been scoped to the three modules the audit named, while **`ostad-profile` (5 screens), `registration-and-verification` (9) and `map-discovery` (1)** had the same gap. All seven ui files now carry loading / empty / error on every navigable screen. Rulings of consequence: **an onboarding stage never discards typed input on a failed save** — the rule for all six stages, since a six-stage wizard over intermittent 4G that loses entries is abandoned permanently; a **failed pause toggle returns to its previous position** rather than appearing to succeed while offers keep arriving; the **suspension notice keeps its Appeal action reachable even when the ticket list fails**, because it is the only screen a banned user can reach and the Terms promise an appeal; **Settings navigation survives a failed `GET /v1/me`**, so logout, legal documents and deletion never become unreachable; a **failed portfolio upload keeps the file queued for retry**; and security-sensitive writes (phone, email, password) **fail visibly rather than replaying silently offline**. Two headings correctly carry no states and are recorded as such: the guest-shell entry points ("there is no standalone welcome screen") and the Shagred card ("a component, not a navigable screen") | Unspecified states are invented at build time and invented differently on each screen; the onboarding forms and the suspension notice are where that costs most. Extends audit G11 | **Baseline v1.3** (pending); OL-OSP-UI-001, OL-REG-UI-001, OL-MAP-UI-001 |
| CL-045 | 2026-09-26 | **Empty / loading / error states added across OFR, RNT and SUP (documentation).** The ui-overview template requires every screen to map loading, empty and error — the offers-and-chat module had **none on any of its six screens**, and RNT and SUP had two each missing. All three files are now at full coverage. Substantive rulings recorded rather than invented: a failed chat send **keeps the message with a retry** instead of discarding what the user typed; an empty sent-offers inbox carries the **Find an Ostad** action, being the Shagred's main activation surface; an empty reviews list shows **"New"** rather than a zero score; a pending Ostad's empty inbox shows review status, not "no offers"; and an empty ticket list shows **Appeal this decision** for a suspended user, since an appeal is their only write | Loading, empty and error are where an unspecified screen gets invented at build time — and the offers-and-chat flow is the product's success unit. Audit G11 | **Baseline v1.3** (pending); OL-OFR-UI-001, OL-RNT-UI-001, OL-SUP-UI-001 |
| CL-044 | 2026-09-26 | **Test/acceptance plan and threat model written (OL-TST-001, OL-THR-001).** NFR-14 covered only Slices 0–3 and left acceptance "in production"; CL-016's bilingual UI had **no review process at all**. OL-TST-001 defines four test layers, **eight critical paths**, per-slice acceptance, bilingual acceptance (a CI check that both locale files carry every key, plus native-speaker review in context), and states honestly what is deliberately not automated. OL-THR-001 models nine threats against six assets, and names the places the spec says "engineering default" where a number is owed: **a per-IP OTP limit (which does not exist — the per-number limit alone lets one source cycle thousands of numbers)**, the viewport cap and scraping rates, and an alert on identity-document views per admin per hour. It records three unverified assumptions, chief among them that **Cloudflare's free-plan rate-limiting capacity has never been checked** though NFR-05 depends on it, and proposes the moderation response targets neither store publishes but both require | "Acceptance in production" is not acceptance, and an untested Bangla half is an untested product. Audit G10 | **Baseline v1.3** (pending); OL-TST-001, OL-THR-001, NFR-14, NFR-09 |
| CL-043 | 2026-09-26 | **App-store release checklist written (OL-STR-001).** Verified against both stores' current policies. Four findings change plans: (a) Google Play **mandates a public web account-deletion URL** in addition to in-app deletion — but accepts *"a customer service email or a form"*, so a **static bilingual page satisfies it and CL-020's read-only website survives**; (b) Apple Guideline 5.1.1(ix) says apps *"that require sensitive user information should be submitted by a legal entity… and not by an individual developer"* — so the developer accounts should be **Technovicinity Limited**, which needs a D-U-N-S number taking **up to 30 days**, making it the longest-lead item in the whole plan; (c) a **reviewer account with a bypassed OTP that works from outside Bangladesh** is required by both stores and is the likeliest repeat-rejection cause for a country-locked app; (d) Apple Guideline 1.2 requires *"a method for filtering objectionable material"* — **the platform has no content filter**, flagged as a v1.3 scope item, not applied | The Slice 5 gate said "app-store compliance checklist passed" with no checklist; store accounts had been an open item since the 2026-09-12 review. Audit G4 | **Baseline v1.3** (pending); OL-STR-001, OL-BLD-001 |
| CL-042 | 2026-09-26 | **Environments, deployment and recovery written (OL-OPS-001).** Defines **two** environments (development and staging, each with its own Supabase project and its own ADR-005 signing key) with the rule that **staging never holds real personal data** — copying production down is how identity vaults leak; CI on every push including a signing-key test against a real Supabase project; and the migration rule that makes rollback safe (**migrations must be backwards-compatible with the deployed API**, so a migration and the code needing it ship as two deploys). Closes the backup gap: **Supabase's database backups do not cover Storage**, so the identity vault, portfolio media, intro videos and chat voice notes had **no backup of any kind** — a daily encrypted copy to a second location is now required, subject to the same purge and legal-hold rules, since a backup that outlives a purge defeats it. The restore rehearsal must restore **both** halves and reconcile them | NFR-07 promised a 24 h RPO/RTO over data that was not being backed up; "staging" and "CI" were named by four requirements and defined by none. Audit G2 and G3, both High | **Baseline v1.3** (pending); OL-OPS-001, NFR-07, NFR-09, NFR-05, NFR-14 |
| CL-041 | 2026-09-26 | **Legal-hold tooling added, and the purge can no longer destroy a live appeal.** Three fixes: (a) **an open appeal pauses the purge** — the sweep now requires `purge_at <= now AND legal_hold IS false AND no open appeal ticket exists`, and an appeal resolved *against* the user pushes `purge_at` to `max(purge_at, resolved_at + 7 days)` so they receive the outcome before the record goes; (b) **appeals link to the latest active `suspend` *or* `terminate` action**, and must name the `terminate` row where the account is terminated; (c) **legal hold becomes operable** — set/release endpoints on the account detail and retention view, both requiring a written reason, with new audit types `legal_hold_set` / `legal_hold_released`, badged wherever a held account is listed. Tooling ships in Slice 4 with ADM-18; until then a hold is a direct database update, as with manual purge (CL-034) | (a) A user terminated on day 0 who appealed on day 28 lost the appeal **and** every route back if an admin had not resolved it by day 30 — the clock silently cancelling the appeal the Terms §7 promise. (b) The appeal link accepted only a `suspend` action, so the one-open-appeal index would refuse a **termination** appeal from anyone who had already appealed the underlying suspension, leaving a banned user with no appeal at all. (c) `legal_hold` sat in the schema and the retention policy, and incident Playbooks 1 and 3 both instruct the responder to place one — with no control anywhere to do it. Audit G5 and G6, both High | **Baseline v1.3** (pending); ADM-18, OL-REG-DM-001, OL-SUP-DM-001, OL-SUP-API-001, OL-ADM-API-001, OL-ADM-DM-001, OL-ADM-UI-001, OL-RET-001, OL-INC-001, OL-BLD-001 |
| CL-040 | 2026-09-26 | **Analytics event catalog and notification catalog drawn out (documentation, no scope change).** Both were already specified — scattered across MAP-11, OFR-08, RNT-10, OSP-12, ADM-12…16, SUP-03 and REG-11 — but never collected, so there was nothing for `POST /v1/events` to validate against despite the REG api instructing it to validate "against the **event catalog**". **OL-CAT-EVT-001** lists every client and server event with properties and the ADM requirement each feeds, fixes the `subject.verb_past` naming contract, and separates emitted events from derived measures. **OL-CAT-NTF-001** lists every push with its mandatory in-app counterpart, and records the deliberate silences (a blocked user and a favourited Ostad are never notified). Two gaps surfaced and are **flagged, not filled**: (G-A) an Ostad is never told a review was left, making RNT-04's one-reply right unusable in practice — a v1.3 scope change needing founder approval; (G-B) suspension revokes push tokens in the same transaction that suspends, so a suspension push is undeliverable by construction — defensible, but currently an accident of ordering rather than a stated decision | The Slice 5 gate and the ADM-12…15 dashboards depend on events that no document enumerated, and OSP-12's acceptance ("insight counts match admin analytics") holds only if both read one source. Audit G9, remaining half | **Baseline v1.3** (pending); OL-CAT-EVT-001, OL-CAT-NTF-001, OL-REG-API-001 |
| CL-039 | 2026-09-26 | **Token signing key corrected — ADR-005 issued, superseding one clause of ADR-004.** ADR-004 had the API sign its JWTs with "the project's Supabase JWT secret (HS256)". Supabase has **deprecated** that legacy shared secret ("No longer recommended") and, since **1 October 2025**, creates new projects on asymmetric signing keys instead — **ours is created in Slice 0, after that cutoff**, so the named key may never be issued to us. The API now signs with an **ES256 key we generate and import** into the project's JWT Signing Keys, holding the private half in the Render environment; tokens carry a **`kid` header** (which ADR-004 omitted, and without which verification is unreliable) and the claim contract `sub` / `role` / `exp`. The `apikey` header takes a **publishable key**, never a minted token. Key rotation becomes a zero-downtime standby→current promotion rather than an event that invalidates live tokens, so Playbook 2 changes shape | Audit M7 asked whether ADR-004's assumption still held. Verified against Supabase's own documentation 2026-09-26: it does not, in the one respect that matters. **The architecture is unaffected** — the API remains the authentication authority, `auth_session` remains the revocation source of truth, and no `auth.users` row is created; Supabase still documents minting your own JWTs, just against a key you import rather than a secret you share. Supabase Third-Party Auth was checked and is unavailable to us (five named vendors, OIDC discovery required, symmetric tokens explicitly unsupported) | **Baseline v1.3** (pending); ADR-005, ADR-004, ADR-001, `api-overview`, OL-INC-001, OL-BLD-001 |
| CL-038 | 2026-09-26 | **Push-token registration separated from push enrollment (spec-coverage fix).** REG-12 said enrollment "is requested during onboarding" — but only Ostads have an onboarding wizard, and the token was written in just two screens (Ostad stage 6, the suspension shell). REG-12 now distinguishes **the OS permission prompt** (once per install: Ostad at stage 6, **Shagred at the end of setup**) from **token registration** (silent, on **every** session start — `register/verify`, every login, every app launch resuming a session — and on every OS token rotation). The login and OTP screens now register; Shagred setup raises the prompt. `device_push_token` needed no change — it already carried `last_refreshed_at` and upsert-in-place | As written, a **Shagred never registered a token at all**, so no offer-accepted or offer-declined push could reach them; and any user on a new or reinstalled device, or returning after a logout, had no token either. The MVP acceptance criterion "Ostad receives a push" and every Shagred offer-outcome push failed on a fresh device. Audit B4 (High) | **Baseline v1.3** (pending); REG-12, OL-REG-UI-001, OL-REG-DM-001 |
| CL-037 | 2026-09-26 | **Launch area named and success targets set (founder decision):** the launch area is **Dhanmondi, Dhaka** — one thana of ~5 km², where seed supply is concentrated and the soft-launch targets are measured. Discovery and registration remain **open across Bangladesh**; nothing is geofenced, and the area governs recruitment and measurement only. New document **OL-MET-001** records the area, the supply/demand/quality targets (40 approved Ostads with all six launch-focus categories covered; offer→acceptance ≥35% as the success unit; zero-result searches <20%), how each is measured from existing instrumentation, and the expansion rule (adjacent thana, after four consecutive weeks on target) | The Slice 5 gate required "funnel metrics reviewed against targets" while no targets existed anywhere. One thana rather than city-wide because a marketplace fails on density: the same 40 Ostads spread over Dhaka's ~306 km² would put ~7 per category city-wide and return nothing at MAP-04's default 5 km radius — the empty map that risk R-01 rates High/High. Founder decision 2026-09-26 (audit G9) | **Baseline v1.3** (pending); OL-MET-001, OL-BLD-001, R-01, S-02 |
| CL-036 | 2026-09-26 | **OTP gateway and email provider selected (founder decision), closing ADR-001 open item 3.** **Alpha SMS** (`sms.net.bd`) for OTP — ৳0.40 non-masked / ৳0.64 masked, ৳1,500 minimum top-up, English REST API, **payable in BDT via bKash/Nagad**. **Resend** free tier for email, which is only ever used to carry a REG-04 verification code. Three consequences beyond procurement: (a) **the OTP SMS template must be written in Bangla** — BTRC has required Bangla bulk-SMS templates since 7 March 2022 (digits, codes and URLs may stay English), and this overrides `preferred_locale`, being a channel regulation rather than a user preference; (b) launch is on a **non-masked numeric sender**, moving to the masked sender ID `OstadLagbo` once BTRC approval lands — masking needs a trade licence, BIN/TIN and a letterhead application, 3–7 business days; (c) **`otp_request` gains `email_verify` plus `account_id` and `email` columns** — REG-04's email verification code had no storage defined anywhere in the data model | International CPaaS was rejected on evidence: ৳34–56/SMS is 60–90× local rates, and GP/Robi/Teletalk have blocked unregistered sender IDs since 8 Sep 2025, so it buys neither cost nor speed; Firebase Phone Auth is $0.20/SMS for Bangladesh and assumes an auth stack ADR-004 discarded. Founder approval 2026-09-26 (audit G8) | **Baseline v1.3** (pending); REG-02, REG-04, ADM-19, ADR-001, R-08, OL-REG-DM-001, OL-BLD-001 |
| CL-035 | 2026-09-26 | **Map tile provider: plan corrected, choice deferred to a named gate (founder decision).** ADR-001's "OpenStreetMap-based via `flutter_map` / no per-load billing" was **wrong on both counts** and is amended. (a) **OSM's public tiles are development-only** — their usage policy forbids commercial products on community-funded servers, forbids prefetching, and states "offline use is not permitted"; dev use must send an app-identifying User-Agent, and they are never shipped. (b) **The basemap must be raster, not vector** — MapLibre, the renderer behind every mobile vector basemap, cannot shape Bengali script, so Bangla labels break; this rules out the free vector options (OpenFreeMap, self-hosted Protomaps). (c) **Tiles become a paid line at soft launch** (≈ $20–30/month; every credible free tier is non-commercial) **or $0 on Google's mobile SDK**, which replaces `flutter_map`. (d) The provider is chosen at a **Slice 0 bake-off before any MAP screen is built**. Separately, **visible map attribution** (`© OpenStreetMap contributors` + the provider's credit) is now a requirement | The spec's tile plan violated OSM policy and under-stated the budget, and the free alternatives that looked obvious all fail on Bangla script — a product-fatal detail for a Bangla-first app. Founder chose to stay free through development and finalise later (audit G1); the Slice 0 gate exists because the `flutter_map`-or-Google half is a screen rewrite, not a config change, once screens exist | **Baseline v1.3** (pending); MAP-02, NFR-13, ADR-001, OL-BLD-001, OL-MAP-REQ-001, OL-MAP-UI-001 |
| CL-034 | 2026-09-26 | **Deletion and incident-playbook sequencing corrected (founder decision):** the **user-facing** account delete (Settings item + `DELETE /v1/me`, immediate deactivation, sessions/push revoked) moves **forward to Slice 1**, where real identity documents first arrive; the automated 30-day purge and retention sweeps stay in Slice 4, with the founder purging manually in the interim. Separately, **Playbooks 1–3 must be operable at soft launch while Playbook 4's dashboard-dependent steps run manually** until its tooling (ADM-10, ADM-12/14/15, ADM-19) ships in Slice 5 | Real Ostads uploaded NID/passport from Slice 1 but had no way to delete until Slice 4 — and the Privacy Policy, consented from Slice 0, already promised self-service deletion. Separately the incident doc was "required before soft launch" while Playbook 4 instructed the responder to use Slice-5 dashboards. Founder decision 2026-09-26 (audit M5) | **Baseline v1.3** (pending); REG-12, OL-BLD-001, OL-INC-001 |
| CL-033 | 2026-09-26 | **One discovery endpoint (founder decision):** `centre` + `radius_km` become optional filters on `GET /v1/discovery/map`, and **`GET /v1/discovery/radius` is removed**. The radius slider is now one more AND filter on the single read that draws the pins, so clustering and the viewport cap apply to it automatically. No server-side `distance_km` — with no list view (MAP-02) nothing needs it; the client computes distance on-device | Two endpoints drew pins with no rule saying which applied, and the radius one was uncapped and cursor-paginated — letting a caller page through every Ostad within 30 km, breaking MAP-DM's own "no single request enumerates more than the cap" rule. Founder decision 2026-09-26 (audit B3) | **Baseline v1.3** (pending); MAP-04, MAP-09, OL-MAP-API-001, OL-MAP-DM-001, OL-MAP-UI-001 |
| CL-032 | 2026-09-26 | **A terminated account disappears from view immediately (founder decision):** the Shagred permitted-Ostad predicate now also requires `deleted_at IS NULL`, which termination sets — so a banned account presents as "deleted account" at once, like a self-deletion, instead of staying fully visible for the whole 30-day appeal window. A **plain** suspension (no `deleted_at`) still stays visible so the Ostad keeps context for a frozen chat or a report; reinstatement clears `deleted_at` and the account reappears. The offers module's "deleted account" display is extended to termination, giving both modules one trigger | Termination keeps `suspended` status, so the status-only clause left a banned user visible to the people they may have harmed for *longer* than someone who left voluntarily — and SGP and OFR disagreed on when the deleted state shows. Founder decision 2026-09-26 (audit B7) | **Baseline v1.3** (pending); SGP-05, OFR-06, OL-SGP-DM-001, OL-SGP-API-001, OL-OFR-DM-001, OL-OFR-API-001 |
| CL-031 | 2026-09-26 | **Gender value set defined (founder decision):** `male \| female \| other` for both `ostad_profile.gender` (required) and `shagred_profile.gender` (optional). The map's gender filter keeps Any / Male / Female — there is **no "Other" filter chip** — so an Ostad stored as `other` is returned only under "Any" | The enum was never defined while MAP-05 already filtered on male/female, leaving the build to guess. "Other" added for inclusivity without changing the family-comfort filter behaviour. Founder decision 2026-09-26 (audit B6) | **Baseline v1.3** (pending); OSP-01, MAP-05, OL-OSP-DM-001, OL-SGP-DM-001, OL-MAP-DM-001, OL-MAP-UI-001 |
| CL-030 | 2026-09-26 | **No `system` chat messages — block/suspension freeze no longer leaks (founder decision):** the platform never inserts a message into a chat on block / suspension / unblock; a freeze is shown by a neutral client banner only. `chat_message.kind` enum → `text \| voice` (removes the third kind that contradicted OFR-05). The failed-send reason and `frozen_reason` are neutralised to `unavailable` for a block or a counterpart suspension (only `deleted` is disclosed), so API opacity holds too | A readable `system` message, and a `blocked` reason, told the blocked party they had been blocked — breaking RNT-08 opacity. Founder decision 2026-09-26 (audit B2) | **Baseline v1.3** (pending); OL-OFR-DM-001, OL-OFR-API-001, OL-OFR-UI-001, RNT-08 |
| CL-029 | 2026-09-26 | **Privacy Policy & ToS v1.1 refresh:** email reveal on connection (CL-011); in-app appeals + 30-day termination window (SUP-04/CL-019); Singapore storage + named processors disclosed (ADR-001/CL-025); support-ticket (2 yr) and non-accepted-offer (12 mo) retention added; reviewer shown by first initial (CL-028); driving-licence handling (CL-027); Bangla at launch (CL-016). Both remain `legal_review: pending` | The user-facing pair (last touched 2026-08-30) had fallen behind the scope and are served for consent from Slice 0 — they must describe the app accurately before real users accept them. Founder-approved 2026-09-26; Bangla authored by founder; Bangladesh legal sign-off before launch | **Baseline v1.3** (pending); OL-PRV-001, OL-TOS-001 |
| CL-028 | 2026-09-26 | **Reviews show the reviewer's first initial only (founder decision):** e.g. "R." — never the Shagred's display name. A stored `rating.reviewer_initial` replaces the live-name / "Former Shagred" display, keeping the "a Shagred is never publicly visible" invariant true | Protects Shagred privacy (no findable name) while keeping reviews credible — chosen over full anonymity. Founder decision 2026-09-26 (audit M2, review visibility) | **Baseline v1.3** (pending); RNT-02, OL-RNT-DM-001, OL-RNT-API-001, OL-RNT-UI-001, OL-RET-001, OL-PRV-001 |
| CL-027 | 2026-09-26 | **Identity is NID or passport only; driving licence reclassified (founder correction):** the driving licence is **not** an identity document — it is a teaching credential collected only from driving instructors, via portfolio (OSP-07 `portfolio_document`). `identity_document.doc_type` enum → `nid \| passport` | The licence proves driving-teaching eligibility, not identity; identity verification must be NID or passport. Corrects CL-017, which had accepted the licence as a third identity option. Founder clarification 2026-09-26 | **Baseline v1.3** (pending); supersedes CL-017's licence-as-identity part; REG-10, OL-REG-REQ-001, OL-REG-DM-001 |
| CL-026 | 2026-09-26 | **Map and favourites are Shagred-only (founder decision):** narrows REG-11 (a pending Ostad views profiles and edits its own, but has no discovery-map tab or favourites) and MAP-08 (favouriting is Shagred-only), matching the UI. Resolves the requirements-vs-UI mismatch | Discovery is a demand-side feature — Ostads are found, they don't search for other Ostads. Founder decision 2026-09-26 (audit §5) | **Baseline v1.3** (pending); REG-11, MAP-08, OL-REG-REQ-001, OL-MAP-REQ-001 |
| CL-025 | 2026-09-26 | **PDPA data-residency deferred to before public launch (founder decision):** Singapore storage of identity documents (ADR-001) is accepted through development; compliance for restricted-category data (in-country storage or legal sign-off) is required before public launch. Caveat: real NIDs are collected from Slice 1, so this carries live-data risk from Slice 1 until resolved (R-10) | Do not block development on the legal ruling, but comply before go-live. Founder decision 2026-09-26 (audit M1) | Decision + pre-launch gate; OL-RET-001, OL-RSK-001 (R-10), README; relates to ADR-001 open item 1 |
| CL-024 | 2026-09-26 | **Banned accounts keep their review weight (founder decision):** a terminated account's ratings stay anonymized and continue to count; admins remove individual fraudulent reviews via `remove_content`. No behaviour change — records the decision so it is not re-flagged | Simpler to build; bulk auto-removal of a banned author's ratings was declined for the MVP. Founder decision 2026-09-26 (audit G7) | No behaviour change (decision record); OL-RET-001, RNT-05 |
| CL-023 | 2026-09-26 | **Unblock does not reopen a frozen chat (founder decision):** a block sets a stored `chat_thread.block_frozen_at` that unblocking does not clear; messaging resumes only after a new accepted offer forms a new connection. Resolves the contradiction where OFR-DM/api said unblock restored writability while RNT-08 / its UI and api said the chat stayed frozen — aligned to RNT-08 | Safety: a blocked-then-unblocked party should not silently regain a live chat; a fresh, consensual connection is required. Founder decision 2026-09-26 (audit B1) | **Baseline v1.3** (pending); OL-OFR-DM-001, OL-OFR-API-001, OL-RNT-DM-001, RNT-08 |
| CL-022 | 2026-09-26 | **Documentation alignment to ADR-004 (auth authority):** api-overview, REG api, and REG data-model rewritten so the **API** — not Supabase Auth — owns the password (`user_account.password_hash`) and mints Supabase-valid JWTs, with **no `auth.users` row**; `auth_session` named the source of truth for refresh/revocation. NFR-05 now permits **scrypt** (user passwords use it per ADR-004). ADR-001 amended (Auth line, dev-OTP line) and marked superseded-in-part by ADR-003/004; ADR-003/004 frontmatter `status` corrected `accepted → current`. **No scope or behaviour change** | The two ADRs accepted 2026-09-25 were never propagated into the older layers, leaving the build agent two conflicting auth designs (audit 2026-09-26, items A1/A2/A3/A5) | **No baseline change (documentation fix)**; OL-API-001, OL-REG-API-001, OL-REG-DM-001, OL-NFR-001, OL-DEC-001, OL-DEC-003, OL-DEC-004 |
| CL-021 | 2026-09-24 | **Profile photo becomes a key field:** after approval, a new photo publishes only when an admin approves it through a revision, like legal names, identity documents, and skills. Alongside it, the key-field revision flow is specified: changes collect in one pending revision the Ostad submits; it is locked while under review; a never-submitted revision may be discarded; an unsubmitted revision inactive for 90 days is discarded with prior notice, its uploaded documents deleted | A verified Ostad could otherwise swap their face for anyone's under the verified badge — the strongest impersonation vector on the platform. Photo changes are rare and quick to review. Surfaced by the OSP api review | **Baseline v1.2**; OSP-01, OSP-10, OL-OSP-DM-001, OL-RET-001, OL-OSP-API-001 |
| CL-020 | 2026-09-14 | **Public website added as a deliverable:** landing/marketing pages and guest-reachable legal documents, hosted on Vercel (ADR-001); guest legal pages built in Slice 2, marketing pages in Slice 5 | ADR-001 created a web surface the baseline's modules never named; logged so it is built with scope, not around it | **Baseline v1.2**; ADR-001, OL-BLD-001, ui layer |
| CL-019 | 2026-09-13 | **Admin account termination:** a `terminate` moderation action that bans an account — it stays suspended for a 30-day appeal window (rule 7: appeal is the only write path), then purges with the retention policy's banned-account exception (ID-number hash, phone, legal name, violation records retained while the ban stands). Suspended users cannot self-delete, so without this action a banned account would sit suspended forever | Surfaced by the cross-layer data-model review: OL-RET-001 promised banned-account retention that no model or requirement could execute | **Baseline v1.2**; ADM-08, OL-ADM-DM-001, OL-REG-DM-001, OL-RET-001 |
| CL-018 | 2026-09-12 | **Admin dashboard requires a TOTP second factor** in addition to email + password | The panel holds identity documents and chat evidence; a single stolen password must not open it (NFR-05) | **Baseline v1.2**; ADM-20, NFR-05 |
| CL-017 | 2026-08-30 | **Identity verification hardened:** driving licence accepted alongside NID and passport; document image requirements become per-type rules (NID front+back · passport photo page · licence front+back); the verification selfie is **live in-app capture only, holding the identity document beside the face** — gallery upload for the selfie is eliminated | The gallery-upload selfie defeated verification's purpose (anyone could upload the card owner's photo); the holding-the-document method gives admin review a three-way face↔photo↔document check at zero ML cost; licence widens the funnel | **Baseline v1.2**; REG-10, OL-REG-DM-001 |
| CL-016 | 2026-08-30 | **Bilingual MVP:** the app ships with full English **and Bangla** UI from launch, replacing "English-first, Bangla post-MVP." Localization architecture from the first commit (all strings externalized, per-locale files, Bangla rendering tested); language chosen at first launch and switchable in settings; English is the source language for specs, Bangla copy authored by the founder; Bangla versions of the Privacy Policy and Terms of Service required at launch. The admin web dashboard remains English-only. | Both languages are core to the market: seed supply (tutors, tradespeople) and guardians need Bangla for onboarding, consent comprehension (PDPA), and trust; a Bangla-first brand with an English-only interface is a mixed message. Founder authors Bangla directly and the AI-driven build maintains dual locale files cheaply, removing the usual simultaneous-launch costs. | **Baseline v1.2**; baseline §7, REG-14, all `ui/` documents when produced |
| CL-015 | 2026-08-30 | **Second-audit clarifications:** registering with a phone inside its 30-day deletion window routes to account recovery; chat messages cannot be deleted or edited by users; no per-category notification preferences in MVP (OS-level control only) | Edge behaviors made explicit so the UI layer and dev AI never guess | **Baseline v1.2**; REG-02, OFR-03/05 |
| CL-014 | 2026-08-30 | **Ostad self-insights (basic):** private counts of profile views, offers by outcome, and connections — counts only, never viewer identities | Supply-side engagement and retention; Ostads deserve to see their funnel | **Baseline v1.2**; OSP-12 |
| CL-013 | 2026-08-30 | **Bangla-script category matching:** every skill category carries an admin-managed Bangla name/alias; typeahead and keyword search match across both scripts with fuzzy tolerance ("গিটার" → Guitar) | Bangladesh-native discovery — a large share of Shagreds search in Bangla | **Baseline v1.2**; ADM-11, OSP-04, MAP-06 |
| CL-012 | 2026-08-30 | **Feature-audit fix package:** consent capture at registration with versioned records and re-acceptance notices (REG-13); guest-reachable Privacy Policy and ToS; ID-number uniqueness across active accounts; Shagred profiles reportable by offer-holding Ostads; logged-in password change; offer inbox screens (OFR-09) | Feature-completeness audit — PDPA consent capture, app-store legal-access rules, identity integrity, and implied-but-unstated screens made explicit | **Baseline v1.2**; REG-06/10/13, MAP-02/03, OFR-01/09, RNT-07, ADM-03 |
| CL-011 | 2026-08-30 | **Verified email revealed mutually on offer acceptance** (where present), alongside phone | Gives the optional email a real job as a second contact channel | **Baseline v1.2**; OFR-04 |
| CL-010 | 2026-08-30 | **Ostad visibility pause toggle:** removes the Ostad from map/search and blocks new offers; existing chats, already-received pending offers, and direct profile access remain, with offers disabled | An Ostad at capacity needs to stop new inbound flow without vanishing on people already engaged | **Baseline v1.2**; OSP-11, MAP-02, OFR-01 |
| CL-009 | 2026-08-30 | **In-app support ticket system added** (`support` module, SUP): Help & Support screen, categorized tickets with private threads and one screenshot, suspension-screen appeal path; admin support queue (ADM-22) | Users need help and appeals in-app; email-only support excludes many Bangladeshi users; ToS §7's appeal promise needs a door | **Baseline v1.2**; SUP-01…06, ADM-22 |
| CL-008 | 2026-08-30 | **Address model revised for both roles:** manual street-address text plus Thana, District, Division, and Postal Code from dropdowns; replaces Division→District→Upazila/Thana→Area. Ostads keep the map pin; Shagreds gain structured address (replacing District+Area coarse location) | Cleaner, dataset-backed address structure; postal code adds locatability without coordinates | Baseline v1.1 §2; OSP-02, SGP-01/02, REG-08 |
| CL-007 | 2026-08-30 | Ostad may post one public reply per review | Fairness — an Ostad's livelihood shouldn't carry unanswered criticism | v1.1 §6; RNT-04 |
| CL-006 | 2026-08-30 | Chat content fixed to text + voice notes (no images/documents/video) | Voice-note culture fit; lower storage and moderation surface | v1.1 §4; OFR-05 |
| CL-005 | 2026-08-29 | Admin panel expanded to full control panel: analytics with charts, demand intelligence, broadcasts, audit log, retention tooling, SMS monitor, directories | Founder requires full business operation and growth control from one panel | v1.1 §5; ADM-01…21 |
| CL-004 | 2026-08-29 | Map discovery additions: gender filter, favorites (private), shareable profile deep links | Family-comfort market dynamics; comparison shopping; WhatsApp-driven growth | v1.1 §3; MAP-05/07/08 |
| CL-003 | 2026-08-29 | Guest browsing: map, search, and profiles open without an account; contact requires registration | Growth — value visible before commitment | v1.1 §3; MAP-03 |
| CL-002 | 2026-08-29 | Portfolio video: exactly one natively stored intro video, max 45 seconds; longer video via external links | Bootstrap cost control with a human touch retained | v1.1 §2; OSP-07 |
| CL-001 | 2026-08-28 | `shagred-profile` module added: minimal fields, private immutable Ostad history, strict visibility lifecycle | Ostads need something to evaluate; Shagreds must never be findable | v1.1 §2; SGP-01…06 |

## Process note

Changes CL-001 through CL-007 were approved during the requirements sessions before this log existed; they are recorded here retroactively and consolidated into baseline v1.1, restoring the baseline as the single truthful answer to scope questions. From v1.1 onward, no scope change is implemented before its entry appears here. Baseline v1.2 was issued 2026-09-25, absorbing CL-009 through CL-021 and consolidating them into the scope prose. A scope freeze is now in effect (OL-BLD-001, principle 4): further changes are logged for v1.3 and are not implemented during the build unless they block a slice gate.
